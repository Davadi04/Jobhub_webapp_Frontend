import { styled, TextField, MenuItem, Button } from "@mui/material";

export const PreviewJobPost = styled("div")({
  width: "100vw",
  height: "fit-content",
  // display: "flex",
  // justifyContent: "center",
  //  alignItems: "center",
  // flexDirection: "column",
});

export const PreviewJobPost_Container = styled("div")({
  width: "1440px",
  height: "fit-content",
  padding: "100px 0px 10px 0px",
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
  backgroundColor: "whitesmoke",
});

export const PreviewJobPost_ContainerBox = styled("div")({
  width: "940px",
  height: "fit-content",
  backgroundColor: "white",
  color: "#667085",
  // borderRadius: "8px",
  padding: "32px",
});

export const PreviewJobPost_ContainerRoleAndLocationBox = styled("div")({
  width: "868px",
  height: "56px",
  marginBottom: "25px",
  display: "flex",
  justifyContent: "space-between",
  alignItems: "center",
});

export const PreviewJobPost_ContainerTypeandDateBox = styled("div")({
  width: "868px",
  height: "56px",
  backgroundColor: "white",
  marginBottom: "20px",
});

export const PreviewJobPost_ContainerTypeBox = styled("div")({
  width: "370px",
  height: "56px",
  display: "flex",
  justifyContent: "space-between",
  alignItems: "center",
});

export const PreviewJobPost_ContainerIndividualTypeBox = styled("div")({
  width: "fit-content",
  height: "24px",
  padding: "2px",
  textTransform: "capitalize",
  color: "#363F72",
  backgroundColor: "#F8F9FC",
  borderRadius: "3px",
});

export const PreviewJobPost_ContainerMainRestVariablesBox = styled("div")({
  width: "836px",
  height: "fit-content",
  marginBottom: "25px",
  // backgroundColor: "#667085",
});

export const PreviewJobPost_ContainerRoleBox = styled("div")({
  width: "200px",
  height: "56px",
  display: "flex",
  alignItems: "center",
  justifyContent: "space-between",
});

export const PreviewJobPost_ContainerMainText = styled("p")({
  fontSize: "16px",
  width: "836px",
  height: "fit-content",
  textTransform: "capitalize",
});

export const PreviewJobPost_ContainerLocationBoxText = styled("p")({
  fontSize: "14px",
  textTransform: "capitalize",
});

export const PreviewJobPost_ContainerPayText = styled("p")({
  fontSize: "16px",
  textTransform: "capitalize",
});

export const PreviewJobPost_ContainerSalaryBoxText = styled("p")({
  fontSize: "18px",
  marginBottom: "25px",
  textTransform: "capitalize",
});

export const PreviewJobPost_ContainerRoleText = styled("p")({
  fontSize: "18px",
  marginBottom: "15px",
  color: "black",
  textTransform: "capitalize",
});

export const PreviewJobPost_ContainerLocationBox = styled("div")({
  width: "150px",
  height: "56px",
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
});
export const PreviewJobPost_ContainerImgBox = styled("div")({
  width: "54px",
  height: "54px",
});

export const PreviewJobPost_ContainerImg = styled("img")({
  width: "100%",
  height: "100%",
});

export const PreviewJobPost_ContainerRoleTextBox = styled("div")({
  width: "132px",
  height: "56px",
});
